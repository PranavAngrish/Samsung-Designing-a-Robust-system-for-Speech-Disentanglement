__base__: ../defaults.yaml
run_name: stage4_robustness

training:
  stage: 4
  batch_size: 64
  num_epochs: 15
  lr: 0.0001
  warmup_steps: 100
  resume_from: checkpoints/stage3_disentangle.pt

data:
  augmentation_prob: 0.9
  hard_neg_fraction: 0.4

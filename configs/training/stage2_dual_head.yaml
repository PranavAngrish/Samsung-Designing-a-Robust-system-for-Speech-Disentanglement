__base__: ../defaults.yaml
run_name: stage2_dual_head

training:
  stage: 2
  batch_size: 128
  num_epochs: 30
  lr: 0.001
  warmup_steps: 500
  resume_from: checkpoints/stage1_backbone.pt

losses:
  orthogonality: 0.0
  adversarial: 0.0

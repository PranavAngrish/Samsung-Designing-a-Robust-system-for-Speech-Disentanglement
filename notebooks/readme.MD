# Notebooks

These notebooks are the original Kaggle experimental training logs used to produce the
final SoloSpeak Stage 7 artifact. They are intentionally preserved as research history,
not as the primary reproducibility interface.

Use the source-controlled pipeline instead:

```bash
python -m scripts.run_production_pipeline --config configs/training/production.yaml ...
```

The final submitted deployable is `exports/solospeak_stage7_deployable_corrected.pt`.
